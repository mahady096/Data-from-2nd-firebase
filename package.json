{
  "name": "dse-data-sync",
  "version": "1.0.0",
  "description": "Sync data from another Firebase project",
  "main": "scripts/sync-data.js",
  "scripts": {
    "sync": "node scripts/sync-data.js"
  },
  "dependencies": {
    "firebase-admin": "^11.10.1"
  }
}
